import { CheckHistoryDirective } from './check-history.directive';

describe('CheckHistoryDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckHistoryDirective();
    expect(directive).toBeTruthy();
  });
});
