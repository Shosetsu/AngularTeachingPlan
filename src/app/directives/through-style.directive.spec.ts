import { ThroughStyleDirective } from './through-style.directive';

describe('ThroughStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ThroughStyleDirective();
    expect(directive).toBeTruthy();
  });
});
